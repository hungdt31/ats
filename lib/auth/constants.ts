/** Tên cookie chứa JWT session (httpOnly). */
export const SESSION_COOKIE_NAME = "ats_session";

/** Thời gian sống token / cookie (giây) — 30 ngày. */
export const SESSION_MAX_AGE_SECONDS = 30 * 24 * 60 * 60;
